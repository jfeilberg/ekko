# Brand intake

Drop new-brand inputs here; I build the brand pack in `brands/<slug>/` from them.

Per brand: fill `brand-input.md`, drop logos in `logos/`, fonts in `fonts/`.
The included `base` pack at `../base/` is the worked example of a finished pack.
