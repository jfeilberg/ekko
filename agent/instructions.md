<!-- Optional. Anything here is appended to the system prompt as an extra
     persona layer (below the framework's safety/formatting rules). Leave
     empty to use the structured persona in agent/persona.ts alone. -->
